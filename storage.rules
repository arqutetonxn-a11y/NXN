rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // ETAPA 1: nenhum upload ou download pelo cliente.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
